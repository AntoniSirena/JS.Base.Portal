import { InfoCurrentPerson } from './info-current-person';

describe('InfoCurrentPerson', () => {
  it('should create an instance', () => {
    expect(new InfoCurrentPerson()).toBeTruthy();
  });
});
