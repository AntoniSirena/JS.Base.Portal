import { InfoCurrentLocators } from './info-current-locators';

describe('InfoCurrentLocators', () => {
  it('should create an instance', () => {
    expect(new InfoCurrentLocators()).toBeTruthy();
  });
});
